# Tactical dashboard / FastAPI extras (install into the rl_tactical env)
# Core deps come from requirements.txt (torch/numpy/pandas/pyyaml/tqdm).
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
python-multipart>=0.0.9
# dev/test
pytest>=8.0
httpx>=0.27
